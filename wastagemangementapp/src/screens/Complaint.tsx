import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Complaint = ({id}: {id: string}) => {
    
  return (
    <View>
      <Text>Complaint</Text>
    </View>
  )
}

export default Complaint

const styles = StyleSheet.create({})